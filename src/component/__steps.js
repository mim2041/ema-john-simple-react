/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > get started > enable sign-in method
 * 6. create sign in and login route
 */

/**
 * 1. Create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set provider
 * */

/**
 * HOSTING
 * One time per PC
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For each project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down) use space bar to select
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory: dist(vite hole)/build
 * 7. single page application: yes
 * 8. continious deployment: no
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 */